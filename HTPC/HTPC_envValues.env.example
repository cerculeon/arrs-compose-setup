# Copy this file to HTPC_envValues.env and adjust values for your environment.

MOVIE_FOLDER=/path/to/media/movies
TV_FOLDER=/path/to/media/tv
INCOMPLETE_DOWNLOADS=/path/to/downloads/incomplete
COMPLETE_DOWNLOADS=/path/to/downloads/complete
WATCH_FOLDER=/path/to/downloads/watch
EBOOK_FOLDER=/path/to/media/ebooks
COMIC_FOLDER=/path/to/media/comics
MUSIC_FOLDER=/path/to/media/music
CONFIG_ROOT=/path/to/config
KIBANA_HOST=192.168.1.20
BEATS_HOST=192.168.1.10
HTPC_DOCKER_COMPOSE_ROOT=/path/to/arrs-compose-setup

# --- Cloudflared (Cloudflare Tunnel) ---
# Obtain your tunnel token from the Cloudflare Zero Trust dashboard.
# Store the real value as a secret (e.g. GitHub Secret: CLOUDFLARED_TUNNEL_TOKEN).
# Never commit the real token to this file.
CLOUDFLARED_TUNNEL_TOKEN=<CLOUDFLARED_TUNNEL_TOKEN>

# --- Lidarr ---
# API key is generated by Lidarr on first run (Settings > General).
# Store the real value as a secret (e.g. GitHub Secret: LIDARR_API_KEY).
# Never commit the real key to this file.
LIDARR_API_KEY=<LIDARR_API_KEY>

# --- Navidrome ---
# Optional: pre-seed the Navidrome admin password on first run only.
# Store the real value as a secret (e.g. GitHub Secret: NAVIDROME_ADMIN_PASSWORD).
# Remove or leave blank after initial setup — subsequent changes are made via the UI.
# Never commit the real password to this file.
NAVIDROME_ADMIN_PASSWORD=<NAVIDROME_ADMIN_PASSWORD>
