# htpc-config
my HTPC configuration backup

main compose file is htpc services, this should be run on B2

raspBerryPiELK.yml should be run on the raspberry pi2b, which runs raspbian OS

the folderVals env-file applies across both machines

the beats containers only work if you update the config manually inside the container, working on it
