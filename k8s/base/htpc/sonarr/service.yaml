---
apiVersion: v1
kind: Service
metadata:
  name: sonarr
  namespace: htpc
  labels:
    app.kubernetes.io/name: sonarr
    app.kubernetes.io/part-of: arrs-stack
    app.kubernetes.io/component: arr
spec:
  selector:
    app.kubernetes.io/name: sonarr
  type: ClusterIP
  ports:
    - name: http
      port: 8989
      targetPort: 8989
      protocol: TCP
